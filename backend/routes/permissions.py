"""
RBAC Permissions System — LEGACY (kept for reference constants only)
All active endpoints moved to routes/perm_groups.py
"""
from fastapi import APIRouter

router = APIRouter()

# No active endpoints — all moved to perm_groups.py
# This file kept only so the import in server.py doesn't break
